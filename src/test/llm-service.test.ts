// SPDX-License-Identifier: AGPL-3.0
import { test } from 'node:test';
import assert from 'node:assert';

test('llm-service: placeholder passes', () => {
  // TODO(Phase 1): test provider selection, API key resolution, streaming
  assert.ok(true);
});
