// SPDX-License-Identifier: AGPL-3.0
import { test } from 'node:test';
import assert from 'node:assert';

test('ls-grep-glob-tools: placeholder passes', () => {
  // TODO(Phase 1): test directory listing, grep semantics, glob expansion
  assert.ok(true);
});
